# Trimmed dependency set for the Vercel serverless deployment — training-only libraries
# (xgboost, lightgbm, mlflow, matplotlib, imbalanced-learn) are dropped since a serverless
# function only ever SERVES predictions from the already-trained model committed in
# saved_models/, it never trains. shap is also dropped: it pulls in numba/llvmlite, which
# alone push the bundle past Vercel's size limit — the code already falls back to the
# model's plain feature_importances_ when shap isn't installed (see app/ml/predict.py),
# so explanations still work, just without per-transaction SHAP attribution.
# psycopg2-binary is dropped too since the default deployment uses SQLite; add it back
# (and set DATABASE_URL) if you point this deployment at a real Postgres instance.
# For local development / training, use requirements.txt instead.
fastapi==0.111.0
sqlalchemy==2.0.30
pydantic[email]==2.7.1
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
python-multipart==0.0.9
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.4.2
